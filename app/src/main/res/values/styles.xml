<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Detail activity theme. -->
    <style name="DetailTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Order list activity themes. -->

    <!-- Style for a tab that displays a campus name -->
    <style name="OrderListTab" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/techPrimary</item>
        <item name="tabSelectedTextColor">@color/techPrimary</item>
        <item name="tabTextAppearance">@style/OrderListTabTextAppearance</item>
    </style>

    <!-- Text appearance style for a category tab -->
    <style name="OrderListTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">@android:color/white</item>
    </style>

</resources>
